{
  "name": "frontend",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "npx --yes serve . -p 3000",
    "build": "echo 'Static site — no build step required'",
    "preview": "npx --yes serve . -p 3000"
  }
}
